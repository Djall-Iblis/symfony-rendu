{% extends 'base.html.twig' %}

{% block title %}CoreClass{% endblock %}

{% block body %}
    <h1>CoreClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ core_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ core_class.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ core_class.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ core_class.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_core_class_index') }}">back to list</a>

    <a href="{{ path('app_core_class_edit', {'id': core_class.id}) }}">edit</a>

    {{ include('core_class/_delete_form.html.twig') }}
{% endblock %}
